# pywin32 — Clipboard (win32clipboard / win32con)
pywin32>=306
# tkinter — Standardbibliothek unter Windows (kein pip-Paket)

# Desktop-Anker-Kalibrierung (--calibrate-anchor): ImageGrab
Pillow>=10.0.0
